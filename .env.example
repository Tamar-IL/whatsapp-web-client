# Root .env.example — copy to .env for local dev convenience.
# All actual config lives in apps/backend/.env (see apps/backend/.env.example).
# Vite reads VITE_* vars from apps/frontend/.env if present.

NODE_ENV=development
