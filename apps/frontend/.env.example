# Frontend dev env. Vite exposes VITE_* vars to the browser at build time.
# In production the backend serves the built bundle, so most of these aren't needed.

# Override only if you're NOT using the Vite dev proxy.
# VITE_API_BASE_URL=http://localhost:3000
